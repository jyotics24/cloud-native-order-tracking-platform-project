# inventory.ini
# Tells Ansible WHERE the target server is and HOW to connect.
# This is the Jenkins EC2 instance created by Terraform.

[jenkins]
100.31.144.32 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/jenkins-key.pem ansible_ssh_common_args='-o StrictHostKeyChecking=no'
